[project]
name = "tab-to-pbi"
version = "0.1.0"
description = "Tableau to Power BI migration tool"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "anthropic>=0.89.0",
    "python-dotenv>=1.2.2",
]

[tool.setuptools.packages.find]
include = ["tab_to_pbi*"]
